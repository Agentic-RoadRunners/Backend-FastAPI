# FastAPI
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.12.5
pydantic-settings==2.3.0
python-dotenv==1.0.1

# Database
asyncpg==0.29.0
neo4j==5.20.0

# MCP (starlette pin: fastmcp 3.x pulls starlette 1.x which breaks FastAPI 0.115)
fastmcp>=2.0.0
starlette>=0.37.2,<0.39.0

# AI / LangGraph (crewai 0.80.0 ile uyumlu versiyonlar)
langchain==1.2.12
langchain-openai==1.1.11
langgraph==1.1.2
langsmith>=0.1.0
openai==2.28.0

# CrewAI (Senaryo B — multi-agent KG enrichment)
crewai==0.80.0
crewai-tools==0.76.0

# setuptools<81 gerekli — crewai telemetry pkg_resources kullanıyor
setuptools<81

# HTTP client (tool calls)
httpx==0.27.0

# Auth
PyJWT==2.8.0

# Utils
python-jose==3.3.0

# Dev / Test
pytest==8.2.0
pytest-asyncio==0.23.0
